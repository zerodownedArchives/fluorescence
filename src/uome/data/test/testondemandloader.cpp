
#include <data/ondemandfileloader.hpp>
#include <data/mapblock.hpp>

#include <boost/shared_ptr.hpp>

int main(int argc, char** argv) {
    //uome::data::OnDemandFileLoader<uome::data::RawMapBlock> loader("/data/games/uo2d/map0.mul");

    //boost::shared_ptr<uome::data::RawMapBlock> block1 = loader.get(0, 196);

    //for (int i = 0; i < 16; i++) {
        //printf("\tblock1->tiles[%i], id=%i z=%i\n", i, block1->tiles_[i].tileId_, block1->tiles_[i].z_);
    //}

    //sleep(2);

    //for (int i = 0; i < 16; i++) {
        //printf("\tblock1->tiles[%i], id=%i z=%i\n", i, block1->tiles_[i].tileId_, block1->tiles_[i].z_);
    //}

    return 0;
}
